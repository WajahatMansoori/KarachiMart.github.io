@model IEnumerable<WebApplication8.Models.OrderDetail>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutBEnd.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderTable.Ord_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.P_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderTable.Ord_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.P_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrDetail_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrDetail_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrDetail_Id })
        </td>
    </tr>
}

</table>
